{
  "name": "subject_detect",
  "description": "Detect subject and content type from manually entered question text",
  "parameters": {
    "type": "object",
    "properties": {
      "questionContent": {
        "type": "string",
        "description": "The question text to classify"
      },
      "studentAnswer": {
        "type": "string",
        "description": "Optional student answer for additional context"
      }
    },
    "required": ["questionContent"]
  }
}
